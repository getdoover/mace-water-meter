[project]
name = "mace-sim"
version = "0.1.0"
description = "Modbus simulator for Mace Agriflow water meter"
requires-python = ">=3.11"
dependencies = [
    "pymodbus>=3.9.2",
    "transitions>=0.9.2",
]
